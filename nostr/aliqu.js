context.lineWidth = 5; // Set the line width to 5 (for example)
context.strokeStyle = 'black'; // Set the stroke color to black (for example)
context.stroke(); // Stroke the shape with the specified line width
